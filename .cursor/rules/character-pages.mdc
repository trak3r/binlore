---
description: Mandatory rules for wiki character pages, including screencap images and formatting
globs: content/characters/*.md
alwaysApply: true
---

# Character Page Conventions

When creating, editing, or auditing character pages in `content/characters/`:

## 1. Mandatory Character Picture
Every character page MUST include an authentic screencap picture directly beneath the primary `# Character Name` heading:

```markdown
# Character Name

![Character Name on Barely Informed News](https://github.com/trak3r/binlore/releases/download/media-assets/<character-slug>.jpg)
```

### Screencap Extraction & Upload Workflow:
1. Extract a clear frame showing the character from their VOD appearance using `binlore screencap` or `tools/binlore_tools/screencap.py` via `ffmpeg`.
2. Save the screencap locally to `tools/screencaps/<character-slug>.jpg`.
3. Upload the asset to GitHub release `media-assets` on repository `trak3r/binlore`:
   ```bash
   gh release upload media-assets --repo trak3r/binlore tools/screencaps/<character-slug>.jpg --clobber
   ```
4. Embed the URL `https://github.com/trak3r/binlore/releases/download/media-assets/<character-slug>.jpg` right under the `# Name` heading.

## 2. Required Structure & Cross-Links
- Frontmatter (`title`, `type: character`, `aliases`, `first_seen`, `status`, `tags: [character, ...]`).
- `# Name` + Image embed directly under heading.
- Overview & Persona.
- Detailed character lore, relationships, backstory, and signature bits.
- `## Appearances` table linking to episodes.
- `## Notable Quotes` and `## Notable Moments`.
- `## Open Questions`.
- `See also:` linking to relevant segments, storylines, or index.
- Always add/update the character's row in `content/characters/index.md`.

## 3. Exclusion of Mentioned Public Figures & Video Clip Subjects
- **Bio pages are strictly reserved for actual on-air characters or in-studio contributors who speak lines in scenes.**
- Public figures, politicians, or celebrities who are merely mentioned, debated, or satirized in news coverage (e.g. Mitch McConnell, Donald Trump, JD Vance, Lindsey Graham, Pete Hegseth) do **NOT** get bio pages.
- People speaking in news video clips, viral videos, or movie clips being watched on the broadcast are external subjects, **NOT** characters, and must **NEVER** be given bio pages or listed as characters.

## 4. Exclusion of Stream Raid Recipients
A "raid" is when a stream concludes and Case sends the viewer audience to another live streamer's channel. These raid recipients are external third parties, NOT part of *Barely Informed News*, and MUST NEVER be tracked as characters or given character pages on the wiki.

## 5. Exclusion of Twitch Chatters & Viewers
Twitch chatters, Discord members, and audience members interacting via text chat (e.g. Card King) are viewers, NOT characters, and must NEVER be given character pages.

## 6. Multi-Episode Storyline Constraint
An event, bit, or incident that only occurred in a single episode is **NOT** a storyline. Storylines must be multi-episode ongoing narrative arcs (e.g. [[../storylines/crum-dick-punch|Crum Dick Punch]], [[../storylines/beyblade-tournament|Beyblade Tournament]]). Single-episode events must never be given pages in `content/storylines/`.
